<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/assets/css/styles.css"/>
  <title>Edit the User</title>
</head>
<body>
  <h1>Edit a Post</h1>
  <form action="/users/<%= post._id %>?_method=PUT" method="POST">
    <label for="title">Title:</label>
    <input type="text" name="title" id="title" value="<%= post.title %>"/>
    <label for="content">Write Post Content Here:</label>
    <input type="text" name="content" id="content" value=" <%= post.content %>" />
    <button type="submit">Update Post</button>
  </form>
  <a href="/posts/<%= post._id %>"> Take you to <%= post.title %></a>
</body>
</html>